// UEFA fixtures are no longer hardcoded here.
// The previous fallback contained stale/incorrect pairings and could create duplicate clubs
// next to the live synchronized ESPN fixture feed. UEFA pages now use synced match data only.
window.UEFA_FALLBACK_FIXTURES={ucl:[],uel:[],uecl:[]};
